version: '2'

services:
  server:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - MAIL=khushiyant2002@gmail.com
      - PASSWORD=Organic-12

  